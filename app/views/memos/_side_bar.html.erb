<div class="rooms">
  <% if user_signed_in? %>
    <% current_user.rooms.each do |room| %>
    <div class="room">
      <div class="room-name">
        <%= link_to room.name, room_memos_path(room) , class: "room-names" %>
      </div>
    </div>
    <% end %>
  <% end %>
</div>